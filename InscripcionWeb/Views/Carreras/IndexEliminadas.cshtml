@model IEnumerable<InscripcionWeb.Models.Carrera>

@{ ViewData["Title"] = "Carreras Eliminadas"; }

<h1>Listado de Carreras Eliminadas</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Restaurar" asp-route-id="@item.Id">Restaurar</a>
            </td>
            <td>
                <a asp-action="EliminacionDefinitiva" asp-route-id="@item.Id">Eliminación Definitiva</a>
            </td>
        </tr>}
    </tbody>
</table>
